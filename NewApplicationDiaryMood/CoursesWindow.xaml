<Window x:Class="UI.CoursesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="CoursesWindow" Height="450" Width="800"
        WindowState="Maximized">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Каталог курсов" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Категория:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="CategoryComboBox" Width="150" SelectionChanged="CategoryComboBox_SelectionChanged"/>
            <Button x:Name="ShowAllButton" Content="Показать все" Width="100" Height="30" 
                    Margin="10,0,0,0" Click="ShowAllButton_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="CoursesDataGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DetailsButton" Content="Подробнее" Width="100" Height="35" 
                    Margin="0,0,10,0" Click="DetailsButton_Click"/>
            <Button x:Name="BackButton" Content="Назад" Width="80" Height="35" 
                    Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
