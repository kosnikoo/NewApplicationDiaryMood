<Window x:Class="NewApplicationDiaryMood.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в Дневник настроения" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Email:" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailTextBox" Text="test@example.com" Height="30" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Пароль:" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordBox" Password="password" Height="30" Padding="5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="LoginButton" Content="Войти" 
                    Height="35" Background="#4CAF50" Foreground="White" 
                    FontWeight="Bold" Margin="0,0,10,0"/>
            <Button x:Name="RegisterButton" Content="Регистрация" 
                    Height="35"/>
        </StackPanel>
    </Grid>
</Window>